{
  "name": "clearGuestAccess",
  "version": "1.0.0",
  "description": "Clears guest-only user records for the current WeChat OpenID",
  "main": "index.js",
  "scripts": {
    "test": "node --check index.js && node --check identity-utils.js && node --test test/*.test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "wx-server-sdk": "3.0.4"
  }
}
